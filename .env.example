# Secret for signing sessions/tokens (generate a random string)
BETTER_AUTH_SECRET=your-secret-at-least-32-chars

# Public base URL for better-auth. Optional on Vercel (uses VERCEL_URL automatically).
# En local conviene fijarlo; en Vercel no hace falta si expones "System Environment Variables".
# BETTER_AUTH_URL=http://localhost:3000

# Turso (optional). If both set, app uses Turso; otherwise SQLite local.
# TURSO_DATABASE_URL=https://<db>-<org>.turso.io
# TURSO_AUTH_TOKEN=
